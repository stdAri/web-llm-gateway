node_modules/
dist/*
# The built userscript is the distribution artifact — Tampermonkey/Greasyfork
# update from its raw URL, so it is committed.
!dist/bridge.user.js
.gateway/
*.log

# Agent tooling — not project source
.agents/
agent/
.claude/
skills-lock.json
